# Optional: URL for the main hero 3D model.
# If omitted, falls back to /car-model.glb (must be present in public/).
# For Vercel, upload car-model.glb to a CDN (e.g. Cloudflare R2, AWS S3)
# and paste the public URL here.
VITE_MAIN_MODEL_URL=https://your-cdn.com/car-model.glb
